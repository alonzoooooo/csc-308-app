{
  "name": "csc-308-app",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "dev": "npm run dev --workspace=react-frontend",
    "dev:frontend": "npm run dev --workspace=react-frontend",
    "dev:backend": "npm run dev --workspace=express-backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alonzo Edwards",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}